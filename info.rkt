#lang info

(define version "0.1")
;(define deps '("pollen"))

; other dependencies:
; '(("ImageMagick" "c:/lib/imagemagick" 'ffi)
;   ("magick.rkt" "ffi/magick" 'local-collection))

; lines of code in my other projects:

; ISBC: 4028
; pechenga-gazeta.ru: 251
; total lisp lines: 2115
; total over all projects: 6946

; total with tests Odysseus: 4627
